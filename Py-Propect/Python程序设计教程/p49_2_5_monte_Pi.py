import math
import random

def montePi(all):
    in = 0
    for i in range(all)
        x = random.random()
        y = random.random()

        distance = math.aqrt(x**2 + y**2)
        if distance <= 1:
            in = in + 1

    pi =  in / all * 4
    return pi

    
